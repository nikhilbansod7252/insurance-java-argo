apiVersion: apps/v1
kind: Deployment
metadata:
  name: insurance-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: insurance-service
  template:
    metadata:
      labels:
        app: insurance-service
    spec:
      containers:
        - name: insurance-service
          image: IMAGE_NAME
          ports:
            - containerPort: 8080
